# API base URL. 미설정 시 src/api/client.ts 의 기본값을 쓴다.
# BE 개발 서버 기동 후 http://localhost:8000 등으로 맞춘다.
VITE_API_BASE=

# LiveKit 서버 URL / API Key / Secret 은 FE 에 두지 않는다.
# 입장 토큰은 서버가 서명해서 POST /interviews/{id}/start 응답으로 내려준다. (BE #9)
